#ifndef LATTICE_H
#define LATTICE_H

/* Function latticePaths returns the amount of paths through a lattice grid
   taking the dimensions of the grid as parameter */
double latticePaths(int dimensions);

#endif
